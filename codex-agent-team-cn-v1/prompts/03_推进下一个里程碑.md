# 03_推进下一个里程碑

把下面整段发给 Codex：

```text
先更新 `docs/exec/current.md` 的 `Progress` 和 `Decision Log`，然后进入下一个 milestone。
继续沿用固定编排：
- codegen
- integration_tester + cr
- summarize

要求：
1. 先明确下一个 milestone 的 objective / touched files / validation / acceptance。
2. 不回头扩写已经通过的 milestone。
3. 不做无关重构。
4. 继续保持：
   - integration_tester 从 unit tests 派生 client integration tests
   - cr 严格抓 fallback / dead code
```
